package server.maps.field;

import server.maps.MapleMap;

public class RN extends MapleMap {
    public RN(int mapid, int channel, int returnMapId, float monsterRate) {
        super(mapid, channel, returnMapId, monsterRate);
    }
}
