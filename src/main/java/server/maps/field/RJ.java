package server.maps.field;

import server.maps.MapleMap;

public class RJ extends MapleMap {
    public RJ(int mapid, int channel, int returnMapId, float monsterRate) {
        super(mapid, channel, returnMapId, monsterRate);
    }
}
