/* 確認能量全開觸發器專用技能 */

var status = -1;

function start(mode, type, selection) {
    qm.forceCompleteQuest();
    qm.dispose();
}
function end(mode, type, selection) {
    qm.forceCompleteQuest();
    qm.dispose();
}
