function start() {
    // var SqlTool = Java.type("database.tools.SqlTool");
    // var qq =  SqlTool.queryAndGet("SELECT * FROM `accounts` WHERE `id` = ?", function (rs) {
    //     return rs.getString("qq")
    // }, [cm.getC().getAccID()]);
    // if (qq === null || qq.isEmpty()) {
    //     cm.getPlayer().dropMessage(1, "您未綁定qq，請綁定qq後再登錄遊戲！");
    //     cm.getC().disconnect(true, false);
    // }
    cm.dispose();
}
